Aula 1B - Botão novo
	setText(true or false);
	setEnable(true or false);

Aula 2B - Botao cancelar
	txtCodigo.requestFocus(); deixa o cursor num campo específico.

Aula 3B - Botão salvar - trabalha com a classe

Aula 4B - Conecta interface com classe
	txtcodigo.gettext();
	if txtcodigo.gettext().equals("") || outros gettext{}
	JOptionPane.showMessageDialog(null, "Todos os campos devem ser inseridos!", "Mensagem", JOptionPane.PLAIN_MESSAGE);

Aula 5B - Trabalhar com tabela
	chama o método para carregar no botão de salvar
	cria método carregatTabelaGerentes(); 
	Classe DefaultTableModel modelo = new DDefaultTableModel(new Object[] {"Cófigo", "CPF", "Nome", "Função"}, 0); #tem que ser fidedgino ao nome das colunas
	for(int i=0; i <listyaGerentes.size(); i++) {
	#Para cada elemento da lista, cria uma linha
	Object linha[] = new Object[]{listaGerentes.get(i).getCodigoGerente(), listaGerentes.get(i).getCPFGerente(), listaGerentes.get(i).getNomeGerente(), listaGerentes.get(i).getFuncao()};
	modelo.addRow(linha); #para adicionar linha

Aula 6B - 
	Para tabela receber modelo:
	tblGerentes.setModel(modelo);
	
	considerando os eventos da tabela:
	útil para editar e excluir dados já cadastrados.
	tblGerentesMouseClicked(){
		int i = tblGerentes.getSelectedRow(): #para selecionar a linha
		id (I<=0 && i < listaGerentes.size()){
			Gerente ger = listaGerentes.get(i);
			
Aula 7B
	Para alterar também considera verificação dos campos, se estão inseridos dados no txt
	Cria variável global
	botao = "editar";
	if (botao.equals("novo")) {quero acionar o construtor}
	else if (botao.equal("editar") {quero editar}
		int index = tblGerentes.getSelectRow();
		listaGerentes.get(index).setCodigoGerente(cod); #faz o msm para os d+

Aula 8b - Exclusão de itens na tabela

Aula 9B - Pesquisa de dados na tabela 
	if (txtCodio.getText().equals("")) {
		JOptionPane para avisar o usuário da inserção
	else {
		Gerente ger;
		int cod = integer.parseInt(txtCodigo.getText());
		for (int i = 0; i < listaGerentes.size; i++) {
			ger = listaGerentes.get(i);
			if (cod == ger.getCodigoGerente()) {
				String codigo = String.valueOf(ger.getCodGerente())
				e faz para o restante	

Aula 10B - Gerencia Clientes 

Aula 11B - Coloca gerente na coluna de clientes

Aula 12B - Seleção de linhas da tabela 

Aula 13B - Classe Conta Bancária - Tela Cadastro de Contas
	Possui a bolinha 
	Mascara
	JformattedTextField - Máscara para forçar o usuário a inserir os valores de acordo com a regra. Ex.: Data, CPF, e-mail

Aula 14B - Criaçao de outra classe para manusear tabela.

Aula 15B - Uniao das classes 


	
	
	

	
